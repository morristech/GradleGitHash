buildscript {
    repositories {
        jcenter()
    }
}


allprojects {
    version = '0.1.2'
    group = 'de.bitdroid.githash'
}


subprojects {
    apply plugin: 'java'

    repositories {
        jcenter()
    }

    task wrapper(type: Wrapper) {
        gradleVersion = '2.3'
    }
}